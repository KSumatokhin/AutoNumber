<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                    xmlns:sys="clr-namespace:System;assembly=mscorlib">

    <!--<converters:EnumToBooleanConverter x:Key="EnumToBooleanConverter"/>-->

    <SolidColorBrush Color="#3f3f46" x:Key="PrimaryBrush"/>
    <SolidColorBrush Color="#f0f0f0" x:Key="SecondaryBrush"/>
    <SolidColorBrush Color="#55555c" x:Key="TertiaryBrush"/>

    <Thickness x:Key="PrimaryMargin">5</Thickness>
    <Thickness x:Key="PrimaryBorderThickness">1</Thickness>


    <Style TargetType="GroupBox">
        <Setter Property="Margin" Value="{StaticResource PrimaryMargin}"/>
        <Setter Property="Padding" Value="5"/>
        <Setter Property="BorderBrush" Value="{StaticResource PrimaryBrush}"/>
        <Setter Property="Foreground" Value="{StaticResource SecondaryBrush}"/>
        <Setter Property="FontWeight" Value="Bold"/>
    </Style>

    <Style TargetType="RadioButton">
        <Setter Property="Margin" Value="{StaticResource PrimaryMargin}"/>
        <Setter Property="Foreground" Value="{StaticResource SecondaryBrush}"/>
    </Style>

    <Style TargetType="CheckBox">
        <Setter Property="Margin" Value="{StaticResource PrimaryMargin}"/>
        <Setter Property="Foreground" Value="{StaticResource SecondaryBrush}"/>
    </Style>

    <Style TargetType="Label">
        <Setter Property="Foreground" Value="{StaticResource SecondaryBrush}"/>
    </Style>

    <Style TargetType="TextBox">
        <Setter Property="Background" Value="{StaticResource PrimaryBrush}"/>
        <Setter Property="Foreground" Value="{StaticResource SecondaryBrush}"/>
        <Setter Property="BorderBrush" Value="{StaticResource TertiaryBrush}"/>
        <Setter Property="BorderThickness" Value="{StaticResource PrimaryBorderThickness}"/>
        <Setter Property="Padding" Value="3"/>
    </Style>

    <Style TargetType="ComboBox">
        <Setter Property="Background" Value="{StaticResource PrimaryBrush}"/>
        <Setter Property="Foreground" Value="{StaticResource SecondaryBrush}"/>
        <Setter Property="BorderBrush" Value="{StaticResource TertiaryBrush}"/>
        <Setter Property="Height" Value="25"/>
    </Style>

    <Style TargetType="Button">
        <Setter Property="Background" Value="{StaticResource PrimaryBrush}"/>
        <Setter Property="Foreground" Value="{StaticResource SecondaryBrush}"/>
        <Setter Property="BorderBrush" Value="{StaticResource TertiaryBrush}"/>
        <Setter Property="BorderThickness" Value="{StaticResource PrimaryBorderThickness}"/>
        <Setter Property="Padding" Value="10,5"/>
        <Setter Property="Margin" Value="{StaticResource PrimaryMargin}"/>
        <Setter Property="Width" Value="80"/>
        <Setter Property="Height" Value="30"/>
        <Style.Resources>
            <Style TargetType="Border">
                <Setter Property="CornerRadius" Value="3"/>
            </Style>
        </Style.Resources>
    </Style>
</ResourceDictionary>